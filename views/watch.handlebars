<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{#if title}}{{title}} | {{/if}}YouTube Viewer</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="icon" href="/dg.svg" />
  </head>
  <body>
    <header class="app-header">
      <div class="container">
        <h1><a href="/">YouTube Viewer</a></h1>
        <div class="search-bar">
          <form action="/" method="get">
            <div class="search-input-container">
              <input
                type="text"
                name="q"
                placeholder="Search YouTube..."
                value="{{query}}"
                required
                aria-label="Search YouTube"
              />
              <button type="submit" aria-label="Search">
                <i class="fas fa-search"></i>
              </button>
            </div>
          </form>
        </div>
      </div>
    </header>
        <main class="container"><div class="video-container">
  <a href="/{{#if query}}?q={{query}}{{/if}}" class="back-button">← Back</a>
  <div class="video-wrapper">
    <iframe 
      src="https://www.youtube.com/embed/{{videoId}}?autoplay=1" 
      frameborder="0"
      allowfullscreen
      title="{{title}}"
    ></iframe>
  </div>
  <div class="video-info">
    <h2>{{title}}</h2>
    <p class="channel">Channel: {{channelTitle}}</p>
  </div>
</div>
    </main>

    <footer class="app-footer">
      <div class="container">
        <p>&copy; {{currentYear}} YouTube Viewer</p>
      </div>
    </footer>

    <script src="/js/app.js" defer></script>
  </body>
</html>